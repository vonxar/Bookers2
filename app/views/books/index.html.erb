
<%= form_for(@book) do |f| %>
<% if @book.errors.any? %>
<div id="error_explanation" style="padding-left: 90px;">
  <h2 style="font-size: 25px;"><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
    <% @book.errors.full_messages.each do |message| %>
  <ul>
    <li><%= message %></li>
  </ul>
  <% end %>
</div>
<% end %>
<% end %>

<div class="container">
  <div class="row">
    <%= render 'layouts/user_info',book:@book , user:@user , name:@users %>



    <div class="col-xs-9">
      <h1>Books</h1>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>Titlev</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @books.zip(@users).each do |book ,user| %>
          <tr>
            <td>
              <%= link_to user_path(book.user_id) do %>
              <%= attachment_image_tag book.user, :profile_image, style: "width: 40px; height: 40px;", format: 'jpeg', class: "img-circle  profile-img", fallback: "no_image.jpg" %>
              <% end %> 
            </td>
            <td><%= link_to book.title , book_path(book) %></td>
            <td><%= book.body %></td>
            <tb><%=  %></tb>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>